services:
  service1:
    build:
      context: ./service1
      dockerfile: Dockerfile
    image: service1:latest
    ports: 
      - "8199:8199"
    networks:
      - internal
      - public
  service2:
    build:
      context: ./service2
      dockerfile: Dockerfile
    image: service2:latest
    ports:
      - "5000:5000"
    networks:
      - internal
  
networks:
  public:
    driver: bridge
  internal:
    driver: bridge
